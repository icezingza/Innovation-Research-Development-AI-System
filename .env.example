# ============================================================
# Cognitive Research Runtime — Environment Configuration
# Copy to .env and fill in values before running.
# NEVER commit .env to version control.
# ============================================================

# --- PostgreSQL ---
POSTGRES_URL=postgresql+asyncpg://cognitive:cognitive@localhost/cognition

# --- Redis ---
REDIS_URL=redis://localhost:6379

# --- Qdrant (vector memory) ---
QDRANT_HOST=localhost
QDRANT_PORT=6333

# --- Neo4j (knowledge graph) ---
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=change_me_in_production

# --- OpenAI-compatible inference ---
# Leave blank to run in heuristic (no-LLM) mode.
# Compatible with: OpenAI, OpenRouter, vLLM, LM Studio, Groq
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com/v1
DEFAULT_MODEL=gpt-4.1-mini

# --- Ollama (local inference, optional) ---
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# --- Embeddings ---
# Use OpenAI embeddings (requires OPENAI_API_KEY) or local sentence-transformers
EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_MODEL_LOCAL=all-MiniLM-L6-v2

# --- Security ---
# Comma-separated API keys for X-API-Key header authentication.
# Leave empty to disable authentication (development only).
API_KEYS=
RATE_LIMIT_ENABLED=false
RATE_LIMIT_REQUESTS_PER_MINUTE=60

# --- Logging ---
LOG_LEVEL=INFO
